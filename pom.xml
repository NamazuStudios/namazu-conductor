<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

  <name>Namazu Elements Conductor</name>
  <description>Multi-platform Container Orchestration System for Namazu Elements</description>
  <url>https://github.com/NamazuStudios/namazu-conductor</url>

  <licenses>
    <license>
      <name>BSD 3-Clause License</name>
      <url>https://opensource.org/licenses/BSD-3-Clause</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <developers>
    <developer>
      <name>Patrick Twohig</name>
      <email>patrick@namazustudios.com</email>
      <organization>Namazu Studios LLC</organization>
      <organizationUrl>https://namazustudios.com</organizationUrl>
    </developer>
  </developers>

  <scm>
    <url>https://github.com/NamazuStudios/namazu-conductor</url>
    <connection>scm:git:https://github.com/NamazuStudios/namazu-conductor.git</connection>
    <developerConnection>scm:git:ssh://git@github.com/NamazuStudios/namazu-conductor.git</developerConnection>
    <tag>HEAD</tag>
  </scm>

  <modelVersion>4.0.0</modelVersion>
  <groupId>dev.getelements.conductor</groupId>
  <artifactId>conductor</artifactId>
  <version>1.0.5-SNAPSHOT</version>

  <packaging>pom</packaging>

  <properties>
    <encoding>UTF-8</encoding>
    <maven.compiler.source>21</maven.compiler.source>
    <maven.compiler.target>21</maven.compiler.target>
    <kotlin.version>2.1.0</kotlin.version>
    <dokka.version>1.9.20</dokka.version>
    <swagger.version>2.2.22</swagger.version>
    <guice.version>7.0.0</guice.version>
    <rs.api>4.0.0</rs.api>
    <jakarta.websocket.version>2.1.0</jakarta.websocket.version>
    <crossfire.version>1.0.4</crossfire.version>
    <servlet.api>6.1.0</servlet.api>
    <logback.version>1.2.3</logback.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.build.timestamp.format>yyyy-MM-dd'T'HH:mm:ss'Z'</maven.build.timestamp.format>
    <elements.version>3.7.22</elements.version>
    <awssdk.version>2.30.0</awssdk.version>
    <api.classifier>${project.groupId}.api</api.classifier>
  </properties>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-jar-plugin</artifactId>
          <version>3.4.2</version>
        </plugin>
        <plugin>
          <groupId>org.jetbrains.kotlin</groupId>
          <artifactId>kotlin-maven-plugin</artifactId>
          <version>${kotlin.version}</version>
          <configuration>
            <jvmTarget>21</jvmTarget>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
    <plugins>
      <!-- Build Source Jars -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-source-plugin</artifactId>
        <version>3.3.1</version>
        <executions>
          <execution>
            <id>attach-sources</id>
            <phase>prepare-package</phase>
            <goals>
              <goal>jar</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <!-- Build Javadoc Jars using Dokka (supports Kotlin sources natively) -->
      <plugin>
        <groupId>org.jetbrains.dokka</groupId>
        <artifactId>dokka-maven-plugin</artifactId>
        <version>${dokka.version}</version>
        <configuration>
          <sourceDirectories>
            <dir>${project.basedir}/src/main/kotlin</dir>
          </sourceDirectories>
        </configuration>
        <executions>
          <execution>
            <phase>prepare-package</phase>
            <goals>
              <goal>javadocJar</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <!--
        Everything in dependencyManagement serves the purpose of centralizing the versions and configurations of
        artifacts used. Every project will refer to this section when including them on the classpath. This is
        configured this way to deliberately avoid confusion over what should be scoped properly.
    -->
  <dependencyManagement>
    <dependencies>
      <!--
                The BOM for Namazu Elements incorporates all the official SDK packages. The packages are provided
                with correct scope and version matching the requested version.
            -->
      <dependency>
        <groupId>dev.getelements.elements</groupId>
        <artifactId>sdk-bom</artifactId>
        <version>${elements.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>software.amazon.awssdk</groupId>
        <artifactId>bom</artifactId>
        <version>${awssdk.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <!--
                The Element's API and Implementation. Note that the API is provided meaning that it will not be packaged
                directly with the Element. Rather it will be added to the bundle as an API dependency. This is important
                when exporting the Element's interfaces to other Elements within the system.
            -->
      <dependency>
        <groupId>dev.getelements.conductor</groupId>
        <artifactId>api</artifactId>
        <version>${project.version}</version>
        <scope>provided</scope>
      </dependency>
      <dependency>
        <groupId>dev.getelements.conductor</groupId>
        <artifactId>api</artifactId>
        <version>${project.version}</version>
        <classifier>${api.classifier}</classifier>
        <scope>provided</scope>
      </dependency>
      <dependency>
        <groupId>dev.getelements.conductor</groupId>
        <artifactId>ecs</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>org.jetbrains.kotlin</groupId>
        <artifactId>kotlin-stdlib</artifactId>
        <version>${kotlin.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <modules>
    <module>api</module>
    <module>ecs</module>
    <module>edgegap</module>
    <module>multiplay</module>
    <module>debug</module>
  </modules>

  <profiles>
    <profile>
      <id>central-publish</id>
      <build>
        <plugins>
          <!-- Central Publishing Plugin -->
          <plugin>
            <groupId>org.sonatype.central</groupId>
            <artifactId>central-publishing-maven-plugin</artifactId>
            <version>0.9.0</version>
            <extensions>true</extensions>
            <configuration>
              <publishingServerId>central</publishingServerId>
              <autoPublish>true</autoPublish>
              <waitUntil>published</waitUntil>
              <waitMaxTime>3600</waitMaxTime>
            </configuration>
          </plugin>
          <!-- GPG Plugin (Required for Maven Central) -->
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-gpg-plugin</artifactId>
            <version>3.2.4</version>
            <executions>
              <execution>
                <id>sign-artifacts</id>
                <phase>verify</phase>
                <goals>
                  <goal>sign</goal>
                </goals>
                <configuration>
                  <gpgArguments>
                    <arg>--pinentry-mode</arg>
                    <arg>loopback</arg>
                  </gpgArguments>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>

</project>
